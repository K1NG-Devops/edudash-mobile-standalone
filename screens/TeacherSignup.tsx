import React from 'react';

// Deprecated screen: teacher sign-up via local mock invitation service
// This component is intentionally a no-op to prevent accidental usage.
// Teacher onboarding should use /(auth)/join-with-code and the redeem-invitation Edge Function.
export default function TeacherSignup() {
  return null;
}
