/**
 * Web Entry Point
 * This file serves as the web entry point for the Expo Router app
 */

import { registerRootComponent } from 'expo';
import App from './App';

// Register the main App component
registerRootComponent(App);

export default App;
