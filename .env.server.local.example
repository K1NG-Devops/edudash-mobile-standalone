# .env.server.local.example - Local server-only secrets for Supabase Edge Functions
# -----------------------------------------------------------------------------
# Copy this file to .env.server.local and fill in real values for local dev.
# Do NOT commit real secrets. .gitignore should already exclude .env* files.
# Use with: supabase functions serve <function-name> --env-file ./.env.server.local

# Base configuration (CLI usually injects these for local dev, but you can set):
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=your-local-anon-key

# Server-only secrets (never expose in client code; no EXPO_PUBLIC_ prefix)
SERVER_SUPABASE_SERVICE_ROLE_KEY=your-local-service-role-key
SERVER_NOTIFY_FUNCTION_TOKEN=your-local-notify-token
SERVER_RESEND_API_KEY=your-resend-api-key

# Optional non-secret sender email for emails function
FROM_EMAIL=noreply@edudashpro.org.za

# Add any future server-only variables below as needed
# SERVER_SOME_API_KEY=...

