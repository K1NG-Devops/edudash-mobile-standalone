export const createSubscription = async (planId: string) => {
  // Placeholder – real implementation remains in current app until migration
  return { success: false, error: 'Not implemented in shared package yet' };
};

